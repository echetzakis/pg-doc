const sandbox = require('sinon').createSandbox();
const cli = require('../../bin/cli');

describe('cli', () => {
    beforeEach(() => {
       // initialize your mocks
    });
    afterEach(() => {
       sandbox.restore();
    });
    describe('when...', () => {
       it('should...');
    });
});
